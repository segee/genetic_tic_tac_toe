#include <stdint.h>

int how_many_moves(uint32_t);
int get_square(uint32_t,int);
int check_for_win(uint32_t);
uint32_t add_a_move(uint32_t, int, int);
void print_board(uint32_t);
int game_done(uint32_t);
